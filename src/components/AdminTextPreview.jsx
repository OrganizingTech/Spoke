import React from "react";

const AdminTextPreview = ({text}) => {
    return (
        <div>
            "I am and Admin Text Preview"
             TEXT PREVIEW: {text}
        </div>
    );
};

export default AdminTextPreview;